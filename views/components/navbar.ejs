<nav>
  <a class="logo" href="/">Members Only</a>
  <% if (locals.currentUser) { %>
    <a href='/logout' class='btn logout-btn'>Logout</a>
  <% } else { %>
    <a href='/login' class='btn logout-btn'>Login</a>
  <% } %>
</nav>
